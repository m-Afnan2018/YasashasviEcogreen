# Root-level env files used by docker-compose.prod.yml - holds real secrets
.env.prod
!.env.prod.example

# Stray Next.js build cache left over from before the client/server/admin split
/.next/

# Local Qodo tool config, not for version control
.qodo/
